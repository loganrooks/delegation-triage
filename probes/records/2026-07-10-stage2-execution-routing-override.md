# POST-MORTEM — 2026-07-10 Stage-2 execution routing override (operator-directed)

- probe_id: P-20260710-stage2-routing-override · type: routing post-mortem (not a paired probe)
- task class / ROUTES row: R5-class work (fully-specified cascade execution) executed by the
  booting session driver rather than routed to an implementer tier
- configs compared (model × effort × surface × harness): driver = fable Cowork session @ session
  effort, Desktop Commander surface; counterfactual/adopted = opus subagent for the mechanical
  remainder
- harness/contract hash (if pinned): n/a (live deploy cascade, not a pinned probe)
- blinded?: no
- frozen tree?: no
- adjudicator: author (this session), operator-directed
- **router:** fable Cowork session @ session effort, Desktop Commander surface — the driver
  continued mechanical cascade execution itself (routing competence is model-conditional, G-02;
  consistent with the fable-driver routing misses already on record)
- **attestation:** (REQUIRED) this Cowork session transcript + operator messages — the operator's
  third routing prompt of the session: *"shouldn't this be done using opus!!!! you are wasting on
  basic tasks"* — which routed the remainder to an opus subagent (this executor).
- verdict: operator override adopted mid-cascade; deliverable unaffected — the live-surgery steps
  (snapshot → repo mint → deploy → archive → hook re-point → TRIAGE/delegation re-point → targeted
  commits) were already complete and committed before the handoff.
- unique catches (per leg): the operator override itself is the catch (external, not self-caught).
- tokens / cost (if observable): not separately metered; the economic point is that the mechanical
  remainder ran at the premium tier when the fallback dominated.
- **tally:** (REQUIRED) feeds KNOWN-WEAKNESSES scarcity-miss → **MINTED at n=2**; count after this
  record: 2/2 (instance 1 = 2026-07-10 survey orchestrator). No W-record flip.
- deviations from clean protocol (named): in-session continuation is defensible **by the letter**
  (durable artifact, in-window cap-bounded, a handoff-risk argument for keeping context in one
  driver) but **dominated on the economics** for the mechanical remainder; the earlier fit line
  even named the counterfactual (route the remainder to opus) and still did not flip until the
  operator directed it — recurrence of the scarcity-miss shape, not a fresh kind.
- record locator(s) + minimal verbatim excerpt(s): this session transcript; operator message
  *"shouldn't this be done using opus!!!! you are wasting on basic tasks"* (third routing prompt).
