# KNOWN-WEAKNESSES — failure kinds in delegated research (open-coded, never force-fit)

_Migrated 2026-07-10 from the predecessor routing table's register (grandfathered prose; locators
re-qualified per the WARRANTS.md KNOWN-REPOS key). Adaptive taxonomy per
bridgewright:experiments/demand-intake/DEMAND-INTAKE-DESIGN.md §6: seeded kinds carry an evidence
grade; ANOMALY entries are unminted observations (mint at ≥2 independent recurrences;
force-fitting an anomaly into a seeded kind is itself an audit failure). Every audit pass updates
this file in the same pass._

| Kind | Definition | Evidence | Route/harness implication |
|---|---|---|---|
| stub-compliance | schema-valid but degenerate/empty task output | observed n=1 (2026-07-02, Workflow StructuredOutput overflow) | anti-stub guard: verify artifacts on disk post-hoc; `report_written` flag |
| provenance-misreport | agent/UI misstates the model or route actually used | observed n=2 (fleet-UI label incident; 2026-07-10 memo self-reporting "fable/high" for an xhigh-inherited run) | transcript JSONL is ground truth; grep `"model":` per lane |
| attribution-drift | content faithful, wrong PERSON attributed | ANOMALY n=1 (2026-07-03 sonnet dev-community lane: HN 46692530 text exact, author wrong) | Tier-1 spot-checks verify author identity, not just content+URL |
| quotemarks-in-paraphrase | quote marks inside `[PARAPHRASE]` items (safe direction; implies verbatim-ness the type disclaims) | ANOMALY n≈2 lanes (2026-07-03 fleet) | candidate lane-contract clause if it recurs |
| quote-fabrication / url-drift / type-mislabel / confirmation-drift | seeded a priori | 0 observed across 16 raw-byte spot-checks (2026-07-03 fleet audit) | priors, not an enum — keep checking |
| coverage-overclaim | a lane asserts window/corpus-level absence from its own partial query yield — a negative claim wearing coverage it doesn't have | observed n=1 (2026-07-05 PROBE-R1-high: "no new eval-awareness entrants this window"; refuted by the paired medium lane's 2606.23583 — caught BECAUSE the probe pair existed) | absence claims scoped "under my queries," never window-level; paired lanes are the live detector |
| interception-conflation | post-redeploy fable benchmark scores classifier-intercepted requests as capability failures | observed n=1 in the field record (RC-22: BridgeBench, 9/12 debugging tasks intercepted, scored 0) [REPORTED — relay; primary 403] | benchmark-ingestion guard: no post-2026-07-01 fable delta enters a warrant without an interception + fallback-configuration check [W-014] |
| summarizer-fabrication | a summarizing search/fetch layer fabricates an association absent from the source | observed live n=1, caught (RC-24: 2605.14153 attributed Sonnet-5 claims; the paper predates Sonnet 5 GA) [CONFIRMED — direct fetch] | raw-fetch verification before propagation; methods reads use a raw-fulltext tool (e.g. arxiv-mcp-pro `download_paper`) by default |
| chart-series-conflation | a reader attributes an adjacent series' datapoint to the wrong model on a dense multi-line figure | ANOMALY n=1 (2026-07-03 card deep-read: opus-4.7's CursorBench curve read as sonnet-5's; caught by figure spot-check) | any delegated claim read off a multi-series CHART gets the figure spot-checked before entering a warrant; text/table claims may sample |
| point-estimate-naivety | a small benchmark delta enters a warrant/gloss with no noise check | ANOMALY n=1 (2026-07-03: "max inversions" within ~1 binomial SE on 150 tasks; operator challenge forced the arithmetic; also caught qualifier-shedding in the chat gloss) | **provisional rule (Class B — ratify or strike):** a quantitative delta enters a warrant only WITH a back-of-envelope noise bound + a variance-availability note; deltas inside ~1 SE report as "flat/indistinguishable"; abstract-level academic claims are hypotheses-to-probe, not routing priors, until the methods are read |
| route-rule-inheritance | a scarcity-context route heuristic gets generalized into a handoff/contract as a bare rule and later followed OVER the routing surface without the conflict surfacing | ANOMALY n=1 (2026-07-03 prix-guesser: "spawn an opus reviewer" traced to cap-conservation reasoning; 3 opus-high gates ran without flagging the fable-high row; operator-caught) | drivers check handoff route prescriptions against ROUTES at FIRST spawn; on conflict, surface both + state which governs [CONTRACT §6.5]; temporary-condition rules carry their condition downstream |
| effort-inheritance over-provisioning | an intended model×effort route executed through a surface that can't pin effort, silently inheriting session effort | **observed n=2** (2026-07-10 gap-refresh orchestrator; 2026-07-10 routing-gaps-survey orchestrator — BOTH stated the trap in the spawn line and proceeded anyway, both operator-caught; record: records/2026-07-10-survey-orchestrator-routing-postmortem.md) | route effort ≠ session effort ⇒ generic spawn is the WRONG surface by construction [CONTRACT §3]; fix minted: the `orchestrator` pin (Claude Code only — in Cowork, where no pin surface exists, surface-can't-deliver is a REASON TO TAKE THE FALLBACK, not a caveat to state); fit lines state effort as *delivered by the surface* |
| scarcity-miss | a route defensible by its row's letter (in-window, enumerated class, durable-artifact test) but dominated by the fallback on the economics the profile encodes — e.g. spending scarce premium capacity when the session already sits at the fallback tier | **MINTED n=2** (2026-07-10 survey orchestrator; 2026-07-10 Stage-2 execution — record: records/2026-07-10-stage2-execution-routing-override.md) (2026-07-10 survey orchestrator: fable pinned from an opus-xhigh booting session, 2 days before window close; fallback dominated — record: records/2026-07-10-survey-orchestrator-routing-postmortem.md) | **tiebreak rule (minted): prefer the fallback/cheaper tier when the work is mechanical-remainder class or the booting session already runs at the fallback tier; an operator routing prompt about cost = flip now, not defend** — Class B, operator may re-word |
| premature-infeasibility | a lane declares a route infeasible / "materially bigger task" without exhausting cheap next steps | observed n=1 (2026-07-10 author-recon rev.1 @ sonnet; operator push → rev.2 solved it at the SAME tier with a better harness — 5×sonnet fan-out + explicit contract. Secondary: the relaying driver endorsed rev.1 without probing) | infeasibility/NOT-OBTAINED claims must enumerate routes tried + the next cheapest untried route; relayed infeasibility is a claim-to-verify; remedy direction: harness-up, not tier-up |

Positive signals, recorded symmetrically: sonnet lanes self-applied the tooling caveat (blanket
PARAPHRASE downgrade through a summarizing fetch layer) and universally disclosed blocked sources
rather than silent-nulling — write-time contract compliance high under the strong harness.
