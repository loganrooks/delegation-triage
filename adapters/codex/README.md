# Codex consumer adapter — Stage-3 note (owed)

Target: a KNOWN-CONSUMERS guidance fragment (AGENTS.md-style) for non-Claude drivers: read
ROUTES.md + STATE.md before delegating to a Claude executor; CONTRACT §3's surface table names
which knobs each surface can pin; expired STATE reads as Unchecked (run `check_state.py`).
